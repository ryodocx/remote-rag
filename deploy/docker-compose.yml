services:
  mcp-server:
    build: ../server/core
    container_name: mcp-server
    volumes:
      # マウント設定: ホスト側のデータをコンテナに引き継ぐ
      - ../data:/app/data
      # HuggingFaceのモデルダウンロードキャッシュをホストと共有（起動ごとの再ダウンロード防止）
      - huggingface_cache:/home/appuser/.cache/huggingface
    command: python -m src.mcp_server.server --transport sse --host 0.0.0.0 --port 8000
    environment:
      - PYTHONUNBUFFERED=1

  caddy:
    image: caddy:2-alpine
    container_name: caddy-proxy
    ports:
      - "8080:8080"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
    depends_on:
      - auth-helper
      - mcp-server

  redis:
    image: redis:7-alpine
    container_name: redis-cache
    ports:
      - "6379:6379"

  auth-helper:
    build:
      context: ../server/auth-helper
    container_name: auth-helper
    environment:
      # CACHE_TYPE: redis or memory
      - CACHE_TYPE=redis
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - OAUTH_INTROSPECT_URL=${OAUTH_INTROSPECT_URL:-}
      - OAUTH_CLIENT_ID=${OAUTH_CLIENT_ID:-}
      - OAUTH_CLIENT_SECRET=${OAUTH_CLIENT_SECRET:-}
    depends_on:
      - redis

volumes:
  huggingface_cache:
